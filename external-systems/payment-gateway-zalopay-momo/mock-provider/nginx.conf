server {
    listen 8080;
    server_name _;
    default_type application/json;

    location = /health {
        return 200 '{"status":"ok","system":"external-payment-gateway-mock"}';
    }

    location / {
        return 200 '{"status":"mock","message":"Local ZaloPay/Momo partner simulation"}';
    }
}

